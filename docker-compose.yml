services:
  bridgebot:
    build: .
    ports:
      - "${PORT:-8588}:${PORT:-8588}"
    env_file:
      - .env
    environment:
      - MEMORY_DIR=/memories
    volumes:
      - memories:/memories
    restart: unless-stopped

volumes:
  memories:
